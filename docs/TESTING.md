# Проверка проекта

## Автоматические проверки

```powershell
npm run build
npm run check
npm test
node scripts/browser-smoke.mjs
```

Первые три команды не требуют интернета и сторонних пакетов. Check проверяет 40 топиков, 2031 карточку с IPA, 48 вопросов диагностики, наполненные подтемы и их тесты, минимум 10 заданий в каждом новом банке, ключи/критерии, зависимости, актуальность Markdown, локальные ссылки и контрольные суммы двух PDF. Отдельно проверяются 26 букв, 44 позиции выбранной фонемной модели, 12 временных форм, расширенные глагольные таблицы, 32 количественные основы, 31 порядковая форма, 7 дней и 12 месяцев, 36 пар существительных, 12 неисчисляемых значений, четыре указателя и семь притяжательных рядов, семь рядов be и 14 вопросительных групп; семь личных рядов Present Simple, 36 показательных глаголов и 12 выражений частотности; семь рядов Present Continuous, 40 основ для -ing и 12 пар значений; три модели there is/are, 16 количественных конструкций, 20 единиц порции и 22 пространственных отношения. Количество не доказывает полноту: содержательный разбор обязателен отдельно.

Проверка переработки 2026-09-21: Node 22.17.0; сборка, валидация и 81 Node-тест проходят. Обёртка установленного npm 12.0.2 / Volta выдаёт MODULE_NOT_FOUND для npm-prefix.js; это проблема локальной установки npm, не зависимости проекта. Выполнены эквивалентные команды напрямую: `node scripts/build-content.mjs`, `node scripts/validate.mjs`, `node --test`. Сервер также запускается через `node scripts/serve.mjs`. Глобальные версии проект не изменяет.

Browser smoke — проверка в установленном Chromium/Chrome/Edge; можно задать `BROWSER_PATH`. Браузер запускается headless с отдельным временным профилем, без обычных пользовательских данных. Проверены: карта → топик → подтема → раздел; черновик и перезагрузка; точное восстановление прокрутки; незавершённый многострочный ответ; отсутствие ключей до отправки; результат по целям и второй вариант; алфавит, звуки, глаголы и числовой справочник; P02 с тремя подтемами, различение 17/1.7, сохранение десятичного ответа и многострочного тестового черновика; P03 с четырьмя подтемами, a/an по звуку, число владельцев и сохранение student’s/students’, длинный черновик, итог и история нового варианта, обе новые таблицы; P04 с тремя подтемами, ролью отвечающего, тремя допустимыми отрицательными формами, различием false/not stated, длинным черновиком, историей варианта B и справочником be; переворот карточки клавишей Enter; IPA и отдельная озвучка без оценки знания; диагностика, маршрут, ручная оценка; мобильная ширина 390 px, включая длинные таблицы и задания. Проверяется, что созданный браузером экспорт проходит тот же валидатор импорта.

Скриншоты в ignored `.artifacts/`: home-desktop, topic-desktop, card-desktop, test-review-desktop, home-mobile, topic-mobile, practice-mobile, card-mobile, p02-topic-desktop, p02-reference-desktop, p02-reading-mobile, p02-reference-mobile, p03-topic-desktop, p03-review-desktop, p03-reference-desktop, p03-reading-mobile, p03-reference-mobile, p04-topic-desktop, p04-review-desktop, p04-reference-desktop, p04-reading-mobile, p04-reference-mobile. Визуально проверены читаемость, иерархия, фокус карточки, отделённые органы управления и отсутствие горизонтального переполнения основного экрана. Распознавание **имитируется**: проверяются согласие и обработка результата, не качество службы. В headless озвучивание может сообщать ошибку — воспроизводимый звук не подтверждён. Временные профили содержат только синтетические ответы; их можно удалить вручную после закрытия браузера.

Дополнительно Node-тесты проверяют миграцию v1 → v2 с сохранением прежних свидетельств, независимость объёма очереди от минут/дней, невозможность зачесть открытый ответ автоматически, историю вариантов, основания ручной оценки, требование прослушанного аудио, защиту от неизвестных ID и повреждённого места продолжения. Для P02 отдельно сверяются независимые ожидаемые значения чисел и часов, past/to, teen/ty, начальные нули, десятичная точка, суффиксы порядковых чисел, полнота заявленного календарного набора, новые карточки и совместимость старого состояния P02. Для P03 — независимые ключи форм, C/U, артиклей, указателей и владельцев; апостроф различает смысл, открытый ответ не зачтён даже при совпадении с образцом; старые заметки/SRS сохранены. Для P04 проверены согласование по главному слову, смена you/I/we, полярность отрицательного вопроса, whose/who’s, смысл вопросительных групп и неизвестных данных, допустимые сокращения, адресная практика по ошибке, полные письменные образцы, старые записи и история теста. Число слов образца проверяется как качество авторского материала, не используется для автоматического зачёта ответа ученика. Парсер авторских заданий отклоняет лишние поля вместо молчаливой потери текста.

Для A101 Node-тесты проверяют независимые ключи -s/-es/-ies, has/does/says и /s z ɪz/, главное слово подлежащего, базу после do/does, обе роли who, you → I/we, различие времени/длительности/частоты и unknown. Неоднозначные модели open и разные позиции sometimes остаются открытыми. Полные образцы 90–120 слов не содержат заглушек; речь не получает автоматической фонетической оценки. Проверяются все 36 форм в карточках, сохранение старых ID/SRS/заметок, неприменимость старой самопроверки к расширенному A101 и сохранение первой попытки после второй.

Браузерный сценарий A101 проходит четыре подтемы, исправление studys/studies, /z/ против /ɪz/, лишнее -s после doesn’t, субъект/объект who, everyday/every day, not always/never и неизвестный сегодняшний факт. Проверены многострочный черновик после перезагрузки, скрытые ключи, два отправленных варианта с неизменной первой попыткой, отдельные экраны истории и нового теста, поиск по 70 строкам приложения и мобильная ширина 390 px. Дополнительные просмотренные скриншоты в `.artifacts/`: a101-topic-desktop, a101-review-desktop, a101-reference-desktop, a101-topic-mobile, a101-reading-mobile, a101-reference-mobile, a101-test-mobile. Данные только синтетические; запись в личный профиль ученика не производится.

Для A102 девять Node-тестов проверяют be, порядок слов и роли кратких ответов, независимые ключи -ing и /ŋ/, dyeing/dying, удвоение и ударение, UK/US варианты, привычку против временного повторения, неизвестные сведения, think/have/taste по значению и роли объекта. Неоднозначные feel/live/see остаются открытыми даже при совпадении с образцом. Проверены полные образцы 80–110 / 100–140 слов, диалоги на 8 и 12 реплик, все 40 форм в карточках, адресное повторение правописания, сохранение старого состояния и обеих попыток без автоматического освоения.

Браузерный сценарий A102 проверяет три подтемы, writting/writing, dying/dyeing, недопустимый положительный краткий ответ Yes, I’m, UK/US формы, временное повторение every day и состояния/действия. Аудиотекст скрыт до содержательной попытки, в том числе после пустой проверки; после ответов доступна расшифровка. Многострочный черновик переживает перезагрузку без раскрытия ключей; после отправки открытые ответы ожидают оценки, второй вариант сохраняет первую попытку. Проверены 69 строк справочника, поиск travelling/traveling и ширина 390 px для топика, чтения, фонетики, приложения, истории и нового теста. Просмотрены скриншоты в `.artifacts/`: a102-topic-desktop, a102-review-desktop, a102-reference-desktop, a102-topic-mobile, a102-reading-mobile, a102-reference-mobile, a102-test-mobile. Реальные ответы ученика не используются.

Для A103 десять Node-тестов проверяют независимые ключи there is/are и it/they, реальные запасы против потребности и неизвестности, счёт порций, loaf/loaves, some/any/no и предложение угощения, сохранение 0.5/1.5/0.75, much/many и few/little по явно заданному смыслу, достаточность и избыток. In/into и on/onto не проверяются механически при допустимых вариантах. Проверены полные образцы 80–110 / 90–120 / 100–140 слов, диалоги на 8–10 реплик, 102 новые карточки, прежние ID/SRS, длинный черновик и неизменность первой попытки после второй. Координаты девяти объектов SVG сверены с текстом и ключами; SVG имеет title/desc, не содержит скриптов или внешних ресурсов, ссылка включена в Markdown. Сервер отдаёт только разрешённый SVG с image/svg+xml, не открывает произвольную папку assets.

Браузерный сценарий A103 проверяет четыре подтемы, ошибочные/верные ответы, сохранение десятичной величины, открытые смысловые варианты, расшифровку аудио после полной попытки, многострочный черновик после перезагрузки без ключей, обе попытки с ручным ожиданием. Проверены 39 строк приложения количества и 30 строк приложения места; на ширине 390 px схема не растягивает страницу, имеет текстовый эквивалент и прокручивается с клавиатуры внутри области с видимым фокусом. Скриншоты в `.artifacts/`: a103-topic-desktop, a103-diagram-desktop, a103-review-desktop, a103-quantity-desktop, a103-topic-mobile, a103-diagram-mobile, a103-reading-mobile, a103-reference-mobile, a103-test-mobile. Визуальная проверка не подтверждает работу реального микрофона или слышимого TTS; ответы синтетические.

CI выполняет build, проверку отсутствия незакоммиченной разницы генерируемых модулей, check и test. Браузер и микрофон в CI не проверяются.

### Общая шкала топиков · 2026-09-22

Сборка, валидация и **93 Node-теста** проходят (12 новых в `test/progress.test.mjs`). Проверены нулевые состояния всех 40 топиков без создания записей, сумма реального объёма разных подтем, только текущие ID и непустые ответы, отличие черновика от отправки, отсутствие прибавки за повторные варианты и отзывы, граница 99/100%, импорт v1/v2 и неизменность исходных данных. Заполнение даже неверных ответов не превращается в mastery; отметка краткой версии не засчитывает расширенную. Счётчик не зависит от времени, карточек и заметок, не закрывает расширенный топик в планировщике.

Браузерный сценарий дополнен проверками:

1. Все 40 топиков имеют шкалу с корректными value/max, доступным названием и описанием; поиск сохраняет правильный счётчик, ID элементов не дублируются. В маршруте используется та же формула.
2. Ввод и очистка ответа обновляют шкалу и счётчик подтемы сразу. Поле и прогресс восстанавливаются после перезагрузки. Черновик теста не даёт отправку; сохранённая попытка добавляет один шаг, открытые ответы остаются «ожидает проверки».
3. Отметка краткого B203 (текущая фикстура) переключает только явно подписанную бинарную шкалу и переживает перезагрузку. Клавиатура раскрывает формулу расширенного топика с видимым фокусом.
4. Отдельная синтетическая фикстура даёт 47%, затем 100% заполнения P01 без подтверждения знаний. Очистка одного ответа показывает 99%; восстановление ответа возвращает 100%.
5. Через настоящие обработчики интерфейса создаётся экспорт Blob, выполняется сброс только изолированного тестового профиля, проверяются нулевые шкалы, затем импортируется тот же JSON через файловое поле. Исходные значения восстанавливаются. Тест перехватывает только сохранение файла на диск и диалог подтверждения; реальные ответы ученика не используются.
6. На ширине 390 px карта, краткий и расширенный топики не растягивают экран. Числа вида «1 / 4» не разрываются. Просмотрены `.artifacts/progress-course-desktop.png`, `progress-topic-desktop.png`, `progress-course-mobile.png`, `progress-topic-mobile.png`, `progress-legacy-mobile.png`.

Для ручной проверки повторите пункты 1–3 на своём ответе, затем экспортируйте резервную копию и сверяйте после импорта. Не сбрасывайте личное состояние без резервной копии. Проверки реального аудио ниже остаются отдельными: этот этап их не подтверждает.

Регрессия боковой панели: на длинной странице результатов примечание не пересекается с меню. При desktop-высоте 600 px панель ограничена высотой окна, последняя ссылка достижима с клавиатуры благодаря внутренней прокрутке. Мобильное меню сохраняет горизонтальную прокрутку. Скриншот: `.artifacts/a101-sidebar-short-desktop.png`.

Регрессия вкладок: разные банки kind=practice имеют различимые подписи. Проверяются «Практика» / «Написание» в A102, «Практика» / «Порции» и «Практика» / «Направление» в A103. Полный заголовок банка остаётся на странице, navLabel меняет только короткую подпись и не меняет опубликованные ID или прогресс.

## Сохранение навигации · 2026-09-22

Сборка, валидация и **101 Node-тест** проходят; `test/navigation.test.mjs` добавляет 8 проверок. Проверены все опубликованные маршруты и разделы, нормализация адреса подтемы, миграция v1/v2 без `navigation`, сохранность прежней закладки, независимые позиции/поля/карточки, ограничения импорта. Черновики не подменяют учебные ответы, оценки и согласия. В серверном тесте проверяется новый точный публичный путь `/navigation-state.mjs`.

`scripts/navigation-smoke.mjs` вызывается из общего browser smoke. Проверены: меню → последняя вложенная подтема с ответом и фокусом; отдельная позиция корня карты и главной; переход сразу после прокрутки без ожидания debounce; поиск/уровень карты и разные поиски в двух приложениях; раскрытые пояснения; браузерные «Назад/Вперёд»; старое упражнение и многострочный ответ диагностики; несохранённый профиль (включая пустое числовое поле); черновик внешнего отзыва без выставления оценки; фраза/транскрипт при выключенном микрофоне и сброшенном согласии; очередь/сторона карточки без изменения SRS. Перезагрузка и открытие адреса без hash восстанавливают экран, явный hash побеждает сохранённый. Проверены прямой корень, неверный адрес, сокращённый путь подтемы и skip-link без порчи маршрута. Реальные обработчики экспорта/сброса/импорта переносят навигацию и черновики.

Ручное повторение:

1. Если сервер уже работал до обновления, остановить его в терминале (`Ctrl+C`) и снова выполнить `node scripts/serve.mjs`: новый allowlist загружается при старте. Не очищать localStorage.
2. В карте установить поиск/уровень, прокрутить; открыть подтему, заполнить ответ и перейти в другой раздел. Меню курса возвращает к подтеме, хлебная крошка карты — к прежним фильтрам и позиции карты.
3. Открыть приложение, ввести поиск; перейти на главную и обратно через меню. Повторить с «Назад/Вперёд» и перезагрузкой. Открытие адреса без hash продолжает последний экран, `#home` явно открывает главную.
4. Проверить незавершённые поля профиля/речи и сторону карточки. Возврат не отправляет тест, не сохраняет оценку профиля/отзыва, не оценивает карточку и не включает микрофон.
5. Экспортировать резервную копию; импортировать и проверить пути/черновики. Не сбрасывать реальное личное состояние без резервной копии.

Просмотрены `.artifacts/navigation-course-desktop.png` и `navigation-speech-mobile.png`; горизонтального переполнения при 390 px нет. Все автоматические действия выполнялись только в отдельном синтетическом профиле. Автоматический перезапуск уже запущенного сервера недоступен в текущей среде; в тесте сервер запускался с обновлённым allowlist.

## A104 и совместимость расширенного топика · 2026-09-22

Сборка, валидация и **113 Node-тестов** проходят. В `test/a104.test.mjs` 12 новых проверок: охват и размеры трёх подтем, отдельные чтения/аудио, варианты и цели тестов, личные ряды, написание и /t d ɪd/, неправильные формы и UK/US варианты, факт/отрицание/неизвестность, роли who, длина полных образцов, карточки, прежние экспорты/SRS и две попытки с pending для открытых ответов. В серверном allowlist проверены все шесть новых файлов данных.

`scripts/a104-smoke.mjs` входит в общий браузерный сценарий. Проверены три подтемы и все 18 банков, многострочный черновик после перезагрузки и возврата через меню, новые варианты A/B, скрытые ключи до отправки, открытые ответы без автоматической оценки, 84 строки приложения и восстановление поиска, шкала 245 шагов, архив прежних drill-ответов без потери SRS. Полный сценарий с прежними проверками прошёл. Просмотрены `.artifacts/a104-topic-desktop.png` и `a104-writing-mobile.png`; автоматическая проверка топика, письма и приложения не обнаружила переполнения страницы на ширине 390 px.

Ручной сценарий перед использованием нового материала:

1. Если сервер запущен до обновления, перезапустить его через `Ctrl+C` → `node scripts/serve.mjs`; не очищать localStorage. Новые публичные файлы разрешаются при запуске.
2. Открыть A104: три подтемы, общая шкала, прежние заметки. При наличии старых drill-ответов раскрыть архив, скопировать ответ; проверить после возврата и перезагрузки. Старый selfChecked не должен давать прогресс новых подтем.
3. Прочитать объяснение, выполнить несколько разнотипных заданий, прерваться посреди текста и вернуться. В тесте оставить многострочный черновик; ключи должны оставаться скрыты до отправки. Открытые ответы после неё — «Ожидает содержательной проверки».
4. В приложении Past Simple найти studied, read, travelled/traveled и все личные ряды. На телефоне прокрутить таблицу внутри её контейнера; текст и поля ответа остаются в пределах страницы.
5. Прослушать аудиотексты выбранным английским голосом и провести настоящий диалог со слушателем. Эти действия не заменяются браузерной имитацией и остаются отдельной ручной проверкой.

## A105: местоимения, планы и взаимодействие · 2026-09-22

Сборка, валидация и **130 Node-тестов** проходят. В `test/a105.test.mjs` 17 новых проверок: четыре естественно разных объёма, охват целей и свежие варианты тестов, семь личных рядов и отсутствие самостоятельного its, формы будущего/can/could, независимо заданные ключи, факты и неопределённость в чтении/аудио, неоднозначные открытые задачи, 14 полных письменных образцов, IPA и карточки, прежний экспорт/SRS и неизменяемые исходные ответы двух попыток. Серверный allowlist разрешает только семь новых публичных файлов данных, не весь репозиторий.

`scripts/a105-smoke.mjs` входит в общий браузерный сценарий. Проверены четыре подтемы и 25 банков, сохранение многострочных черновиков после перезагрузки и возврата через меню, скрытые ключи, варианты A/B и ожидание содержательной оценки. Старый selfChecked и архив не добавляют шагов; шкала имеет 332 шага. Оба приложения (7 и 43 строки) сохраняют поиск, прежние карточки не получают новую оценку SRS. Проверка топика, письма и обоих приложений не обнаружила переполнения на ширине 390 px. Просмотрены `.artifacts/a105-topic-desktop.png` и `.artifacts/a105-writing-mobile.png`. Все ответы синтетические, реальное аудио не оценивалось.

Ручной сценарий перед использованием материала:

1. Перезапустить старый сервер через `Ctrl+C` → `node scripts/serve.mjs`, не очищая localStorage; открыть A105 и проверить четыре подтемы, архив, заметки и общую шкалу.
2. Пройти разные банки, оставить многострочный ответ и перейти в другой раздел. Меню и перезагрузка возвращают к прежнему вложенному экрану без потери работы.
3. В итоговом тесте сохранить незавершённый текст и вернуться. До отправки ключей нет; после отправки открытые ответы ожидают проверки. Второй вариант не добавляет отдельный шаг общей шкалы.
4. В приложениях найти hers, going to, could и when I arrive. Проверить адресата, форму и смысл в контексте; на мобильном таблица прокручивается внутри контейнера.
5. Отдельно прослушать четыре аудиотекста, проверить слышимое различие can/can’t и провести диалог с человеком. Автоматический сценарий не подтверждает произношение, микрофон или работу внешней ASR-службы.

В отличие от прежнего зафиксированного сбоя, на этом этапе `npm.cmd run build`, `npm.cmd run check` и `npm.cmd test` успешно выполняются. npm 12.0.2 предупреждает о неподдерживаемой версии Node 22.17.0; глобальные версии не менялись. Прямые эквиваленты Node также проходят.

## A201: Present Perfect Simple · 2026-09-23

Сборка, валидация и **148 Node-тестов** проходят. `test/a201.test.mjs` добавляет 18 проверок: четыре разных объёма и свежие тесты по всем целям, семь личных рядов/40 V2–V3/14 временных опор, независимые ключи форм/маркеров/чтения/аудио, конкретные расчёты длительности, допускающие варианты открытые задания, 16 полноразмерных письменных моделей, 80 карточек и сохранение прежних ID/ответов/SRS. Отдельно зафиксировано: запрос отмены или возврата при неизвестном результате — не доказанное выполнение и не доказанный отказ.

`scripts/a201-smoke.mjs` включён в общий browser smoke. Проверены четыре подтемы и 25 банков, неправильный/правильный ответ, скрытый до полной проверенной попытки транскрипт, многострочный черновик после перезагрузки и возврата через меню, отправка обоих вариантов каждого теста с сохранением двух исходных попыток и ожиданием ручной оценки. Старый архив не добавляет шаги; у A201 342 шага. Проверены все 61 строка приложения и сохранение поиска, прежние карточки без новой оценки SRS, отсутствие переполнения на ширине 390 px. Просмотрены `.artifacts/a201-topic-desktop.png`, `a201-writing-mobile.png`, `a201-reference-mobile.png`; дополнительно сохранены `a201-present-perfect-desktop.png`, `a201-topic-mobile.png` и `a201-reading-mobile.png`.

Ручной сценарий перед использованием материала:

1. Перезапустить старый сервер через `Ctrl+C` → `node scripts/serve.mjs`, не очищая localStorage. Открыть A201, проверить четыре подтемы, прежние заметки и архив коротких ответов.
2. Выполнить несколько задач из разных банков; оставить длинный ответ посреди абзаца, перейти в другой раздел и вернуться. Продолжение должно сохранять вложенный маршрут и текст.
3. В тесте оставить многострочный черновик; до отправки ключей нет. Отправить полную попытку и затем второй вариант: обе истории сохраняются, открытые ответы ожидают проверки, повторный вариант не добавляет отдельный шаг шкалы.
4. В приложении найти written, since, got/gotten и личные формы. На телефоне горизонтально прокрутить таблицу внутри контейнера, не растягивая страницу.
5. Прослушать четыре аудиотекста и контекстный пример с read /red/. Проверить различимость have/haven’t со слушателем и провести интервью Have you ever…? → When did…? Транскрипт и синтетические ответы не подтверждают произношение.

`npm.cmd run build`, `npm.cmd run check` и `npm.cmd test` выполняются с предупреждением о несовместимости версий npm/Node. Реальный микрофон, внешняя ASR-служба и слышимое TTS остаются непроверенными автоматическим сценарием.

## A202: описание, сравнение и достаточность · 2026-09-23

Все **160 Node-тестов** проходят. `test/a202.test.mjs` добавляет 12 проверок: естественные объёмы 80/86/84, новые тесты по всем целям, независимые ключи adjective/adverb и сравнения, варианты форм, направление as…as, позиции enough, числовые данные и отсутствие подтверждения, 36 рядов/20 конструкций приложения, девять полных письменных моделей, 72 карточки, старый архив/SRS и две неизменяемые попытки. Смысловые неоднозначности не проверяются единственной строкой.

`scripts/a202-smoke.mjs` включён в полный browser smoke. Проверены три подтемы и 21 банк, ошибочный/верный ответ, скрытый аудиотекст до полной проверенной попытки, многострочные черновики после перезагрузки и возврата через меню, оба варианта каждого теста, pending у открытых ответов и обе исходные попытки в истории. Шкала — 253 шага; повторные варианты и старый архив ничего не добавляют. Проверены 56 строк приложения, восстановление поиска, неизменность SRS и мобильная ширина 390 px. Просмотрены `.artifacts/a202-topic-desktop.png`, `a202-writing-mobile.png`, `a202-reference-mobile.png`; дополнительно сохранены `a202-comparison-desktop.png`, `a202-topic-mobile.png`, `a202-reading-mobile.png`.

Ручной сценарий:

1. Перезапустить старый сервер (`Ctrl+C` → `node scripts/serve.mjs`), не очищая localStorage. Открыть A202: три подтемы, 250 заданий, старые заметки и read-only архив ответов better/carefully/enough.
2. Проверить формы hard/hardly, well о здоровье, quieter/more quiet, farther/further и направление not as wide. Свободное обоснование выбора должно оставаться на содержательной проверке.
3. Оставить абзац в практике и многострочный черновик теста; уйти в другой раздел, вернуться и перезагрузить. Текст и вложенный путь сохраняются. Отправить тест и второй вариант: история содержит обе попытки, процент не увеличивается за повторный вариант.
4. Найти в приложении enough, good/well, friendly и far. На телефоне прокручивать широкую таблицу внутри контейнера, без горизонтального растяжения всей страницы. Проверить карточки-выражения, их перевод/IPA и отдельную кнопку озвучивания.
5. Прослушать три новых аудиотекста, сопоставить их с ключами. В паре проверить hard/hardly, cheaper/cheapest, enough/not enough и живой выбор с изменением числа участников. Только прослушивание настоящего ответа позволяет оценить произношение.

Микрофон, внешняя ASR-служба и слышимое качество TTS этим автосценарием не подтверждены. npm.cmd выводит прежнее предупреждение о версиях npm/Node, сами проверки выполняются.

## A203: правила, советы и условия · 2026-09-23

Все **176 Node-тестов** проходят. `test/a203.test.mjs` добавляет 16 проверок: естественные объёмы 84/80/98, покрытие целей новыми вариантами, независимые ключи must/have to/need to, запрет против необязательности, advice/advise, should как совет/ожидание, had better и предложения вариантов, if/when/unless, until/by и границы вывода. Проверены нормативные сокращения, 15 полноразмерных моделей письма, факты чтения и аудио, семь личных рядов/44 модели, 78 карточек, сохранение исходных legacy-ответов/SRS и обеих попыток с pending для открытой работы.

`scripts/a203-smoke.mjs` проходит в составе полного browser smoke: все три подтемы и 22 банка, ошибочный и правильный ответ, скрытый транскрипт до полной проверенной попытки, многострочный черновик после перезагрузки и возврата через меню, два варианта каждого теста с неизменной историей. Шкала содержит 265 шагов; повторный вариант и архив не добавляют выполненную работу. Проверены 51 строка приложения и сохранение поиска, прежние карточки/SRS, мобильная ширина 390 px. Просмотрены `.artifacts/a203-topic-desktop.png`, `a203-writing-mobile.png`, `a203-reference-mobile.png`; также сохранены `a203-rules-conditions-desktop.png`, `a203-topic-mobile.png`, `a203-reading-mobile.png`. Все ответы синтетические, отдельный временный профиль браузера.

Ручной сценарий:

1. Перезапустить старый сервер (`Ctrl+C` → `node scripts/serve.mjs`) без очистки localStorage. Открыть A203: три подтемы, 262 задания, старые заметки и read-only архив вопросов о arrives, запрете курения и необязательном раннем приходе.
2. Проверить разницу mustn’t/don’t have to и shouldn’t/mustn’t; на странице условий сравнить until/by и вопросительное when. Свободное обоснование должно оставаться на содержательной проверке, а не оцениваться совпадением с образцом.
3. Оставить длинный ответ в практике и многострочный черновик теста, уйти в другой раздел и вернуться. Перезагрузить: вложенный путь, текст и место сохраняются. Отправить оба варианта: две исходные попытки остаются, открытая работа не получает автоматической оценки, процент не растёт за повторную отправку.
4. В приложении найти mustn’t, unless, until и whether. На телефоне прокрутить таблицу внутри контейнера. На карточках проверить целостный смысл give it a try и play it by ear, IPA и отдельное озвучивание.
5. Прослушать три аудиотекста и сопоставить детали с ключами. Со слушателем проверить различимость should/shouldn’t, will/won’t и advice/advise. В диалоге изменить одно условие и проверить, смог ли ученик перестроить план; одного чтения образца недостаточно.

Реальные микрофон, ASR и слышимое качество TTS автоматическим сценарием не подтверждены. npm.cmd выдаёт прежнее предупреждение о версиях npm/Node; проверки выполняются.

## A204: фон, рассказ и relative clauses · 2026-09-23

Все **192 Node-теста** проходят. `test/a204.test.mjs` добавляет 16 проверок: объёмы 82/92/96, свежие тесты всех целей, 15 моделей по 100–140 слов, независимые ключи was/were/-ing/кратких ответов, процесс/состояние/ограниченное отрицание, время/причина/уступка, субъект и объект relative clause, нормативные альтернативы и пропуск с сохранением предлога, whose/where/согласование и область утверждения. Проверены конкретные времена чтения/аудио, неизвестная причина и результат, явно отрицаемое владение против неизвестного, две таблицы, 84 карточки, старый архив/SRS и история двух попыток.

`scripts/a204-smoke.mjs` проходит в общем browser smoke: три подтемы и все 23 банка, неверный/верный ответ, скрытый транскрипт, многострочный черновик после перезагрузки и возврата через меню, оба варианта тестов без автоматических оценок открытой работы. У топика 273 шага; повторные варианты и прежние ответы не повышают новый прогресс. Проверены поиск и восстановление состояния двух приложений (27 и 24 строки), мобильная ширина 390 px. Просмотрены `.artifacts/a204-topic-desktop.png`, `a204-writing-mobile.png`, `a204-relative-reference-mobile.png`; также сохранены `a204-past-narrative-desktop.png`, `a204-relative-clauses-desktop.png`, `a204-topic-mobile.png`, `a204-reading-mobile.png`, `a204-reference-mobile.png`. Данные только синтетические, профиль браузера изолирован от ученика.

Ручной сценарий:

1. Перезапустить старый сервер (`Ctrl+C` → `node scripts/serve.mjs`) без очистки localStorage. Открыть A204: три подтемы, 270 задач, старые заметки и read-only архив вопросов was/who/because.
2. Проверить Was/Were you…? → I was, lying/travelling/traveling; сравнить because/although и who called/who I called. Свободные объяснения и неоднозначный выбор должны оставаться на содержательной проверке.
3. Оставить абзац практики и многострочный черновик теста, перейти в другой раздел и вернуться; затем перезагрузить. Вложенный путь и данные сохраняются. После двух отправок история содержит обе исходные попытки, открытая работа pending, процент не растёт за второй вариант.
4. Найти while и because в приложении повествования, whose, object и preposition в relative clauses. На телефоне прокрутить таблицы внутри контейнера; проверить карточки-выражения, IPA, переворот и отдельное озвучивание.
5. Прослушать три аудиотекста; проверить времена 17:40 и 08:25, число помощников и признак коробки. Со слушателем проверить отрицания was/wasn’t, were/weren’t, смысловые паузы, whose/who’s в контексте и диалог с уточнением предмета. Омонимичное написание ASR не является фонетической ошибкой.

Реальные микрофон, внешняя ASR-служба и слышимость TTS не подтверждаются имитацией. npm.cmd выводит прежнее предупреждение о несовместимости версий, но команды проходят.

## A205: модели действий, фразовые глаголы и договорённости · 2026-09-24

Все **208 Node-тестов** проходят. `test/a205.test.mjs` добавляет 16 проверок: неравные объёмы 88/82/96, покрытие целей свежими тестами, 15 моделей письма по 100–140 слов, независимые ключи -ing/to, объектных местоимений, stop/remember/try, порядка частиц, поиска/находки, отмены/переноса и хранения/выбрасывания. Отдельно проверяются at/on/in/нулевой предлог, UK/US weekend, in/for/during, by/until, роли говорящего/адресата, полный смысл отказа, факты и неизвестное в чтении и аудио, три приложения и 84 карточки. Прежние шесть исходных карточек и put up with остаются со стабильными ID; старые ответы, заметки, SRS, черновик и две попытки проходят round trip schemaVersion 2 без переноса зачёта.

`scripts/a205-smoke.mjs` входит в общий browser smoke: 22 банка, ошибочный/верный ответ, скрытый аудиотекст до полной проверенной попытки, многострочный черновик теста после перезагрузки/возврата через меню, обе попытки трёх подтем и pending открытой работы. Шкала имеет 269 шагов, повторные тесты и архив не добавляют новые. Проверены поиск и восстановление во всех трёх приложениях (28/30/30 строк), адаптивность на 390 px. Сценарий использует только синтетические данные и отдельный профиль браузера.

Ручной сценарий:

1. Перезапустить старый сервер (`Ctrl+C` → `node scripts/serve.mjs`), не очищать localStorage. В A205 три подтемы, 266 задач, старые заметки и read-only архив reading/to/turn it off.
2. Проверить enjoy making, want to learn, look forward to meeting; сравнить stop doing/to do и try to/doing. Для like/start и свободного смыслового выбора варианты не должны оцениваться по единственной строке.
3. Проверить turn it off против look for it; put away/throw away, put off/call off. Уточнить, что инструкция и предложение не доказывают выполнение. Старый архив не переносится в новые ответы.
4. В календаре проверить on Friday evening, next Sunday без предлога, in the next three weeks, UK at/US on the weekend, by/until и in/for/during. В приглашении проверить полный отказ I’d love to, but… и не считать отсутствие ответа согласием.
5. Оставить длинный текст, перейти в другой раздел, вернуться и перезагрузить; проверить путь, поля и историю двух тестов. Открытые ответы ждут оценки; второй вариант не увеличивает заполнение топика.
6. Найти remember, away и until в приложениях. На телефоне прокрутить таблицы внутри контейнера. Проверить карточки с идиомами, IPA, переворотом и отдельным озвучиванием. Сохранены `.artifacts/a205-topic-desktop.png`, `a205-verb-patterns-desktop.png`, `a205-phrasal-patterns-desktop.png`, `a205-time-arrangements-desktop.png`, а также мобильные topic/writing/reading/reference/phrasal-reference/time-reference. Визуально проверены desktop-топик, мобильное письмо и таблица фразовых глаголов.
7. Прослушать независимые аудиотексты и проверить 13:30, 45 минут, noon/Thursday, статус ремонта и переноса. В паре проверить частицы, can/can’t, thirteen/thirty и уточнение дня. ASR не заменяет слуховую оценку.

Реальный микрофон, внешняя ASR-служба и слышимое качество TTS не проверены автоматически. npm.cmd продолжает предупреждать о версиях npm/Node, но сборка и проверки выполняются.

## B101: деятельность, предыстория и привычки · 2026-09-24

Все **223 Node-теста** проходят. `test/b101.test.mjs` добавляет 15 проверок: неравные объёмы 108/96/94, все цели в новых тестах, 15 полных моделей по 130–170 слов, независимые ключи have/has/been/-ing, V3/had, вопросов и сокращений, for/since, результата/процесса/состояния, хронологии и неизвестного. Проверяются три used-модели, рамка would, пассивная цель, UK/US got/gotten и spelling, самостоятельные чтение/аудио, таблицы и 96 карточек. Старые ID, архив, SRS, черновик и обе попытки сохраняются без наследования зачёта; 100% работы оставляет открытые ответы ожидающими оценки.

`scripts/b101-smoke.mjs` прошёл в составе полного сценария: 22 банка, неверный и верный ответы, скрытый транскрипт до полной проверенной практики, многострочное письмо после перезагрузки, возврат во вложенную страницу через меню, оба варианта теста и pending всех открытых задач. После синтетической выборки 15 ответов + три отправленных теста шкала показывает 18/301, второй вариант и архив не добавляют шаги. Три приложения имеют 25/27/24 строки; поиск сохраняется. Проверены 390 px и отсутствие горизонтального переполнения страницы, таблицы прокручиваются внутри контейнера.

Ручной чек-лист:

1. Перезапустить старый сервер без очистки localStorage. У B101 три подтемы, 298 задач и архив waiting/had/getting up с исходными вопросами; новая практика не заполнена старыми ответами.
2. Сравнить have been working / have worked, have known, только что прекращённую деятельность, for/since и отрицание sleeping well. Вывод о качестве или завершении требует фактов.
3. В предыстории восстановить реальные события и часы. Проверить before I had finished, допустимый Simple после before/after, had had, ’d had/would и read /red/.
4. Сравнить used to work / be used to working / get used to working. Не считать привычность удовольствием, did use to слуховой орфографической задачей, а is used to cut ошибкой.
5. Ввести несколько абзацев, уйти в другой раздел и вернуться, перезагрузить, отправить оба теста. Открытое письмо/речь ждут содержательной проверки; история исходных ответов сохраняется.
6. Проверить приложения по since/before/would, мобильные таблицы и карточки. Снимки `.artifacts/b101-topic-desktop.png`, `b101-writing-mobile.png`, `b101-phrasal-reference-mobile.png` просмотрены; последний показывает Past Perfect. Остальные снимки: три desktop-приложения, topic/reading/reference/time-reference mobile.
7. Реально прослушать речь ученика и TTS, проверить have/has/been, had/hadn’t, read и used to. Транскрипт ASR не заменяет аудио; качество внешней службы и физический микрофон остаются непроверенными в автоматическом сценарии.

Тесты используют только синтетические ответы и отдельный профиль. Предупреждение npm о версиях npm/Node сохраняется; команды выполняются.

## B102: условия, желания и возможность · 2026-09-24

Все **239 Node-тестов** проходят. `test/b102.test.mjs` добавляет 16 проверок: объёмы 100/84/90, покрытие целей новыми тестами, 15 полных моделей письма по 130–170 слов, независимые ключи форм и смыслов. Проверены was/were, unless/only if/in case/even if, условная дистанция без процентов, wish/hope и другие модели wish, had + имя/V3, may not, can/must и выводы. Отдельно проверены факты и неизвестное в чтении/аудио, две таблицы, 84 карточки и сохранение семи прежних ID, архива, SRS, черновика и двух попыток.

`scripts/b102-smoke.mjs` прошёл в полном browser smoke: 22 банка, неверный/верный ответ, скрытый транскрипт, многострочное письмо после перезагрузки и возврата через меню, оба теста и ожидание ручной проверки всех открытых ответов. Шкала имеет 277 шагов; синтетическая выборка из 13 ответов и трёх тестов даёт 16/277, повторный вариант не увеличивает заполнение. В приложениях 29/32 строки, фильтр восстанавливается. Мобильная ширина 390 px без переполнения страницы; таблицы прокручиваются внутри.

Ручной чек-лист:

1. Перезапустить старый сервер, не очищая localStorage. B102 показывает три подтемы и 274 задачи; архив were/had/do not с прежними вопросами доступен read-only, старая отметка не заполняет новые задания.
2. Проверить if + past / would + base, вопросы и отрицания; was/were в заданном контексте; tomorrow при past form не превращается во вчера. If = whether и will готовности не исправляются вслепую.
3. Сравнить необходимое only if, unless с собственным not, заранее in case и even if. Отсутствие ответа не трактуется как отказ или согласие.
4. Сравнить I wish I knew / could / they would…, hope и пожелание I wish you luck. Для открытых вариантов нет оценки по одной строке, знания языка не угадываются по одной локальной трудности.
5. Различить may not возможности и запрета, must вывода и правила, might not и can’t, общую возможность can и конкретную догадку. Не приписывать модальным фиксированные проценты.
6. Ввести несколько абзацев, уйти и вернуться, перезагрузить, отправить оба варианта. Проверить исходные ответы, историю, SRS и незавышенный прогресс.
7. Проверить поиск unless/wish в приложениях и карточки с IPA/идиомами. Просмотрены `.artifacts/b102-topic-desktop.png`, `b102-writing-mobile.png`, `b102-wishes-reference-mobile.png`; сохранены также desktop-приложения и мобильные topic/reading/reference.
8. Реально прослушать would/wouldn’t, wish/which, may/might и отрицания; проверить независимые числа/дни аудиотекстов. Только настоящее аудио позволяет оценивать речь.

Физический микрофон, внешняя ASR-служба и слышимое качество TTS не проверены автоматически. Сценарий использует отдельный профиль и синтетические ответы; npm.cmd предупреждает о версиях, но команды выполняются.

## B103: пассив, процессы и услуги (2026-09-24)

Build/check и все **257 Node-тестов** проходят. `test/b103.test.mjs` добавляет 18 проверок: 96/92/84 задачи, 28/26/24 примера, свежие тесты с покрытием целей, 15 полноразмерных моделей письма, независимые ключи. Проверены согласование и V3, роли местоимений, by/with, непереходные arrive/happen и looked after, been/being, модальность и статус, do-support услуги, have/get с исполнителем, неприятные события и UK/US варианты. Чтение и аудио различают прямое противоречие и отсутствие сведений.

`scripts/b103-smoke.mjs` прошёл внутри полного browser smoke: все 22 банка, ошибочный/верный ответ, скрытый аудиотекст до проверки, многострочное письмо после перезагрузки и возврата через меню, два варианта теста каждой подтемы и ручной статус всех открытых ответов. Для B103 шкала — 275 шагов; синтетические 13 ответов практики и три отправленных теста дают 16/275, повторные варианты не добавляют шагов. Архив sent/must/The room is cleaned. и SRS сохраняются. Текущая фикстура краткого топика — B104.

Ручной содержательный и визуальный чек-лист:

1. Сопоставить active/passive на одном событии: роли и время должны сохраняться, be согласуется с новым подлежащим.
2. Сравнить by-agent, with-tool и by-deadline. Машина может быть исполнителем или средством по контексту.
3. Проверить is being checked / has been checked / must be checked; проведённый тест не обязательно пройден успешно.
4. Сравнить had it repaired / had repaired it, have a person do / get a person to do, had a wallet stolen. Не обвинять пострадавшего по грамматической форме.
5. В длинном ответе оставить несколько абзацев, уйти в другой раздел, вернуться и перезагрузить; после отправки оригинал должен сохраниться, открытые ответы — ожидать проверки.
6. Проверить поиск being/have в приложениях (35/26 строк), возвращение фильтра, отдельную озвучку карточек и сохранность старых ID.
7. Просмотрены `.artifacts/b103-topic-desktop.png`, `b103-writing-mobile.png`, `b103-services-reference-mobile.png`. Мобильная страница не переполняется; широкая таблица прокручивается внутри. Остальные снимки B103 также сохранены в ignored .artifacts.

Тестовый профиль синтетический; личные ответы не использованы. Физический микрофон, внешняя ASR-служба и слышимое TTS не проверялись. npm.cmd выполняет команды, но предупреждает о сочетании npm 12.0.2 / Node 22.17.0. Для новых публичных файлов перезапустить сервер без очистки браузерного прогресса.

## B104: пересказ, косвенные вопросы и question tags (2026-09-24)

Build/check и все **275 Node-тестов** проходят. `test/b104.test.mjs` добавляет 18 проверок: неравные объёмы 100/104/98, 28/30/28 примеров, свежие тесты всех целей, 15 полных письменных моделей и независимые ключи. Проверены say/tell/explain, backshift и контекстные варианты, реальные участники/даты, модальность и границы утверждения, порядок вложенных вопросов, отрицания и who, просьбы/предложения, вспомогательные и особые местоимения tags, ответы yes/no. Проверяется сохранность всех семи прежних карточек и 84 новых с IPA; 32/36 справочных моделей не заявляются исчерпывающей грамматикой.

`scripts/b104-smoke.mjs` прошёл в составе полного browser smoke: три подтемы, все 24 банка, неправильный/правильный ответ, скрытый аудиотекст до полной проверенной практики, оба варианта каждого теста и ручная проверка открытых ответов. Многострочный черновик переживает перезагрузку и уход/возврат через меню. Архив told/is/don’t остаётся read-only; заметки и SRS не меняются. 305 шагов — заполнение, не mastery; старый selfChecked и повторный вариант не дают новых шагов. Остающаяся краткая фикстура — B105.

Ручной чек-лист:

1. Сверить роли в say to / tell / explain to, актуальное is/lives после said, разные значения must. Не исправлять любой допустимый пересказ исключительно по механической таблице.
2. Указать исходных говорящего и адресата, день и место. Проверить, что tomorrow не переносит событие при чтении сообщения позже, а lived не объявляет работу завершённой.
3. Проверить Could you tell me where it is?, отрицательное why she didn’t reply, разные who called / who I called, whether to и if will в значении whether.
4. В пересказе различить вопрос, просьбу, предложение помощи, предложенный вариант и подтверждённое действие. Отсутствие ответа не превращать в отказ.
5. Проверить basic tags, everyone/nobody → they, nothing → it, there, aren’t I / am I и let’s → shall we. Imperative и lexical have оценивать с учётом вариантов. Yes, I have после отрицательного вопроса означает совершённое действие.
6. Прочитать длинные ответы по смыслу, сохранить исходный текст и отзыв отдельно. Речь и интонацию не оценивать по одному ASR-транскрипту; выполнить отложенное применение на новом материале.
7. Проверить поиск must/whether в приложениях, возврат фильтра и карточки с отдельной озвучкой. Просмотрены `.artifacts/b104-topic-desktop.png`, `b104-writing-mobile.png`, `b104-services-reference-mobile.png` (таблица questions-tags). Мобильная страница не переполняется, таблица прокручивается внутри.

Использован только изолированный синтетический профиль. Физический микрофон, внешняя ASR-служба и слышимое TTS остаются непроверенными. npm.cmd выдаёт предупреждение о npm 12.0.2 / Node 22.17.0, но команды завершаются успешно. Для новых публичных файлов перезапустить сервер без очистки прогресса.

## B105: связный текст, лексика и переписка (2026-09-24)

Build/check и все **296 Node-тестов** проходят. `test/b105.test.mjs` добавляет 21 проверку: объёмы 84/92/86/94, 26/28/28/28 примеров, новые варианты всех целей, 20 полных моделей по 130–170 слов, независимые ключи и допустимые варианты. Проверены звуки перед a/an, определённость/U/C/согласование, референты, формы связок и пунктуация, структура абзаца, причинность и scope, сочетаемость/предлоги, части речи/написание/ложные друзья, ударение, формы просьб и статусы переписки. Отдельные чтение/аудио проверяются по конкретным именам, количеству, версиям, датам и неизвестным фактам.

`scripts/b105-smoke.mjs` прошёл в полном browser smoke: четыре подтемы, 30 банков, неправильный/правильный ответ, скрытый транскрипт до проверенной попытки, оба итоговых варианта и ожидание оценки открытой работы. Многострочный черновик сохраняется после перезагрузки и ухода/возврата через меню. Архив make/development/без артикля и SRS сохраняются; 360 шагов не включают прежний selfChecked и повторные варианты. Краткая фикстура — B201.

Ручной чек-лист:

1. Проверить артикль по знаниям адресата, the feedback как конкретное U, list of changes is, a useful/an honest/a one-page/an IT. Не выбирать референт it по одному ближайшему слову.
2. В редактуре абзаца сохранить факты, роль примера, ограничения и неизвестность. Topic sentence не обязана быть первой; But в начале не запрещён. Проверить although/despite, because/because of, however и пунктуацию.
3. Сверить make/do, take a decision/conduct research, discuss без about и a discussion about, reply to/answer. Производное должно занимать подходящее место, а actually и sensible — сохранять значение.
4. Прослушать develop/development и photograph/photography в настоящей речи. ASR-написание и картинка IPA сами не подтверждают ударение.
5. В письме проверить предмет/версию, действие, срок и зону. Received не approved; проверка первой страницы не всего файла; предложение срока не agreement. Подтвердить роль адресата ответом, не угадывать по молчанию.
6. Оценить длинное письмо по цели, фактам, связности, языку и регистру. Сохранить исходник и правку отдельно; провести диалог и отложенное применение на другом материале.
7. Проверить поиск despite/research/confirm в трёх приложениях и возврат фильтра; старые карточки и отдельную озвучку. Просмотрены `.artifacts/b105-topic-desktop.png`, `b105-writing-mobile.png`, `b105-words-reference-mobile.png`. Мобильная страница не переполняется; таблицы прокручиваются внутри.

Профиль синтетический, личные ответы не использованы. Физический микрофон, внешняя ASR-служба и слышимое TTS требуют ручной проверки. npm.cmd завершает команды, но предупреждает о npm 12.0.2 / Node 22.17.0. Для новых файлов перезапустить сервер без очистки прогресса.

## B201: прошлые и будущие опоры — 2026-09-24

Build/check и все **314 Node-тестов** проходят. `test/b201.test.mjs` добавляет 18 проверок: 82/82/92 задачи, 26/26/30 примеров, свежие варианты по всем целям, 12 полных моделей по 180–220 слов. Независимые ключи проверяют цепочки had been/will be/will have/will have been, отрицания/вопросы, V3 и UK/US, состояние/процесс/результат, прошлую и будущую арифметику, by/until/not until, временное и вопросительное when/if. Открытые смысловые альтернативы остаются открытыми. По конкретным данным проверяются архив, репетиция, план клуба, перенос звонка, миграция и каталог.

`scripts/b201-smoke.mjs` прошёл в полном браузерном сценарии: три подтемы и 21 банк, ошибка/верный ответ, скрытый аудиотекст, многострочный черновик и вложенный маршрут после перезагрузки/возврата, оба теста с ручным ожиданием. Архив finished/working/waiting, заметки и SRS сохраняются. 259 шагов не включают старый selfChecked, черновики и повторные варианты. Краткая фикстура — B202. Проверены поиск и восстановление фильтров в трёх приложениях, desktop и 390 px без переполнения страницы.

Ручной чек-лист:

1. Для had been doing найти прошлую опору, не выводить завершение, непрерывность или провал; различать отрицание long/well/regularly и полное отсутствие занятия.
2. Для будущего кадра не приписывать точное начало; различать план использования, разрешение, предложение и подтверждение. When как время не путать с косвенным вопросом.
3. Проверить continuing state в will have known/owned, допустимые live/work, будущее начало накопленного периода, разницу been here/checked/checking и нормальный I’ll finish by Friday без Perfect.
4. Пересказать прогноз, сохранив if/expect, конкретный объём, источник и неизвестные исходы. Не смешивать копирование/проверку, завершение теста/успех, проверку/одобрение.
5. Оценить письмо по рубрике и фактам, сохранить исходник, провести новый диалог и отложенную проверку. Произношение длинных цепочек, will/won’t и read /red/ оценивать только по реальному аудио.
6. Сверить карточку estimate /ˈestɪmeɪt/ с глагольным контекстом и прежним ID; проверить общие слова, идиомы, phrasal verbs и отдельную озвучку.

Просмотрены `.artifacts/b201-topic-desktop.png`, `b201-writing-mobile.png` и `b201-email-reference-mobile.png` (последний содержит таблицу Future Perfect). Личные ответы не использованы. Реальный микрофон, внешняя ASR-служба и слышимое TTS требуют отдельной проверки. npm.cmd предупреждает о сочетании npm 12.0.2 / Node 22.17.0; глобальные инструменты не изменялись. Перезапустить старый сервер без очистки браузерных данных.

## B202: контрфакты, сожаления и выводы — 2026-09-24

Build/check и все **331 Node-тест** проходят. `test/b202.test.mjs` добавляет 17 проверок: 102/84/86 задач практики, 30/26/28 примеров, шесть свежих вариантов по всем целям. Независимые ключи проверяют V3, вопросы/отрицания, had/would, mixed time frames и смысловой have, инверсию, UK/US, модальную силу и область отрицания, необходимость/факт/ожидание/сожаление. Все 18 письменных моделей соответствуют указанному в задании объёму, 12 из них — 180–220 слов. Открытые альтернативы не получают автоматический зачёт.

`scripts/b202-smoke.mjs` прошёл в полном браузерном сценарии: три подтемы и 22 банка, ошибка/верный ответ, скрытый аудиотекст, сохранение многострочного черновика и вложенного пути после перезагрузки/возврата, оба теста в истории, ручное ожидание открытых ответов. Старый архив would/had/left, заметки и SRS сохраняются. 275 шагов не включают старую отметку, черновик и повторные варианты. Текущая краткая фикстура — B203. Проверены две таблицы (32/38 строк), поиск и восстановление фильтров, desktop и 390 px без переполнения страницы.

Ручной чек-лист:

1. Для каждой части mixed conditional определить время отдельно; различить would have a copy и would have had a copy. Проверить реальные факты до вывода об альтернативе.
2. Сохранить could/might, не объявлять контрфакт доказательством единственной причины. Even if не сообщает, выполнили ли условие; неизвестный прошлый факт не обязательно ложен.
3. Различить must have, had to, might not have и can’t have; не приписывать фиксированные проценты. Проверить, не подменены ли доступ действием, отсутствие записи отсутствием события, молчание отказом.
4. В wish/if only отличить прошлое от нынешнего состояния, принять wish could have по смыслу. Should have оценивать как совет или ожидание по контексту; будущее время опоры допустимо.
5. Для needn’t have установить выполненное ненужное действие; для didn’t need to / didn’t have to искать отдельное свидетельство выполнения. Не превращать позднее предупреждение в информацию, доступную раньше.
6. Оценить письмо по рубрике с цитатами, сохранить исходник, провести новый диалог и отложенную проверку. Have/of, ’d, read /red/ и интонацию проверять по реальному аудио, не по ASR-тексту.

Просмотрены `.artifacts/b202-topic-desktop.png`, `b202-writing-mobile.png` и `b202-modal-reference-mobile.png`. Личные ответы не использованы. Реальный микрофон, внешняя ASR-служба и слышимое TTS требуют ручной проверки. npm.cmd предупреждает о npm 12.0.2 / Node 22.17.0; глобальные версии не изменялись. Старый сервер нужно перезапустить без очистки браузерных данных.

## Ручная проверка аудио на компьютере ученика

1. `npm start`, открыть localhost в браузере с поддержкой SpeechRecognition.
2. Открыть речь, выбрать UK/US в профиле. Прослушать образец; убедиться, что выбран английский голос. При отсутствии голоса должно появиться сообщение, а не ложный успех.
3. Без согласия микрофон не запускается. После согласия и разрешения ОС/браузера произнести фразу, остановить; проверить транскрипт.
4. Запретить разрешение — понятная ошибка. Выключить интернет — понятное сообщение или сетевая ошибка, возможность вручную вставить текст сохраняется.
5. Перейти в другой раздел/вкладку — распознавание должно остановиться. При возвращении не включается само.
6. Сравнение текста подписано как сходство слов, не фонетическая оценка. Проговорить числа и сокращения: ASR может представить их иначе.
7. Проверить живой разговор и запись со слушателем. Только этот этап позволяет оценить понятность произношения.

Физический микрофон, слышимое качество TTS и доступность внешней службы нельзя подтвердить тестом с имитацией. До ручного выполнения этих шагов они считаются непроверенными в данной среде.

## Ручная проверка данных

Экспортировать JSON → сохранить резервную копию → импортировать → сверить ответ, карточку и черновик. Импорт заменяет состояние только после проверки и подтверждения. Повреждённый/неподдерживаемый JSON должен быть отклонён без потери текущих данных. «Скачать исходное хранилище» позволяет сохранить непрочитанный исходник при проблеме загрузки. Сброс касается только ключа приложения, не файлов на диске.

В приватном режиме/при запрете localStorage приложение должно сообщить об отсутствии сохранения. При смене порта данные не переносятся автоматически: используйте экспорт/импорт. Никогда не используйте реальные ответы ученика как тестовые фикстуры в Git.
